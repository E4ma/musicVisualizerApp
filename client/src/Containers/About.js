import React from 'react'

const About = () => {
    return (
        <div>
            <h3>About...</h3>
            <p>
                Normally BUTTON components will render a HTML BUTTON element.
                However you can render whatever youd like, adding a href prop will automatically render an element.
                You can use the as prop to render whatever your heart desires.
                React Bootstrap will take care of the proper ARIA roles for you.
                <br />
                <br />
                    Normally BUTTON components will render a HTML BUTTON element.
                    However you can render whatever youd like, adding a href prop will automatically render an element.
                    You can use the as prop to render whatever your heart desires.
                    React Bootstrap will take care of the proper ARIA roles for you.
            </p>
        </div>
    )
}

export default About
